apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "quoteservice.fullname" . }}-test-connection"
  labels:
    {{- include "quoteservice.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "quoteservice.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
