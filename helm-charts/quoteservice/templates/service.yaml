apiVersion: v1
kind: Service
metadata:
  name: {{ include "quoteservice.fullname" . }}
  labels:
    {{- include "quoteservice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "quoteservice.selectorLabels" . | nindent 4 }}
