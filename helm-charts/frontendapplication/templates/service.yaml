apiVersion: v1
kind: Service
metadata:
  name: {{ include "frontendapplication.fullname" . }}
  labels:
    {{- include "frontendapplication.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "frontendapplication.selectorLabels" . | nindent 4 }}
